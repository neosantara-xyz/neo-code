# NAI Code

NAI Code (Neosantara Code) is a Neosantara-first terminal coding assistant.

```bash
nai login
nai --provider neosantara --model grok-4.1-fast-non-reasoning
nai --list-models neosantara
```

Credentials are stored in `~/.neosantara-code/agent/auth.json`.
