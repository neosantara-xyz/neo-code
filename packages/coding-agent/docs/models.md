# Models

The built-in provider is `neosantara`.

Default model:

```txt
grok-4.1-fast-non-reasoning
```

Add or adjust Neosantara model IDs in `packages/ai/src/models.generated.ts` or in your `models.json` provider config.
