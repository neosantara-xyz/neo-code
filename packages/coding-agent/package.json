{
	"name": "@neosantara/code",
	"version": "0.76.76",
	"description": "Neo Code coding agent CLI",
	"type": "module",
	"bin": {
		"neo": "dist/cli.js"
	},
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		},
		"./hooks": {
			"types": "./dist/core/hooks/index.d.ts",
			"import": "./dist/core/hooks/index.js"
		}
	},
	"files": [
		"dist",
		"docs",
		"examples",
		"CHANGELOG.md"
	],
	"scripts": {
		"clean": "shx rm -rf dist",
		"dev": "tsgo -p tsconfig.build.json --watch --preserveWatchOutput",
		"build": "tsgo -p tsconfig.build.json && shx chmod +x dist/cli.js && npm run copy-assets",
		"build:binary": "npm --prefix ../tui run build && npm --prefix ../ai run build && npm --prefix ../agent run build && npm run build && bun build --compile ./dist/bun/cli.js --outfile dist/neo && npm run copy-binary-assets",
		"copy-assets": "shx mkdir -p dist/modes/interactive/theme && shx cp src/modes/interactive/theme/*.json dist/modes/interactive/theme/ && shx mkdir -p dist/modes/interactive/assets && shx cp src/modes/interactive/assets/*.png src/modes/interactive/assets/*.svg dist/modes/interactive/assets/ && shx mkdir -p dist/core/export-html/vendor && shx cp src/core/export-html/template.html src/core/export-html/template.css src/core/export-html/template.js dist/core/export-html/ && shx cp src/core/export-html/vendor/*.js dist/core/export-html/vendor/ && shx mkdir -p dist/releases && shx cp ../../releases/NOTES.md dist/releases/NOTES.md",
		"copy-binary-assets": "shx cp package.json dist/ && shx cp README.md dist/ && shx cp CHANGELOG.md dist/ && shx mkdir -p dist/theme && shx cp src/modes/interactive/theme/*.json dist/theme/ && shx mkdir -p dist/assets && shx cp src/modes/interactive/assets/*.png src/modes/interactive/assets/*.svg dist/assets/ && shx mkdir -p dist/export-html/vendor && shx cp src/core/export-html/template.html dist/export-html/ && shx cp src/core/export-html/vendor/*.js dist/export-html/vendor/ && shx mkdir -p dist/releases && shx cp ../../releases/NOTES.md dist/releases/NOTES.md && shx cp -r docs dist/ && shx cp -r examples dist/ && shx cp ../../node_modules/@silvia-odwyer/photon-node/photon_rs_bg.wasm dist/",
		"test": "vitest --run --testTimeout=20000",
		"prepublishOnly": "npm run clean && npm run build"
	},
	"dependencies": {
		"@neosantara/agent-core": "^0.76.76",
		"@neosantara/ai": "^0.76.76",
		"@neosantara/tui": "^0.76.76",
		"@silvia-odwyer/photon-node": "^0.3.4",
		"chalk": "^5.5.0",
		"diff": "^8.0.2",
		"glob": "^13.0.1",
		"highlight.js": "^10.7.3",
		"hosted-git-info": "^9.0.2",
		"ignore": "^7.0.5",
		"jiti": "^2.7.0",
		"minimatch": "^10.2.3",
		"proper-lockfile": "^4.1.2",
		"typebox": "^1.1.24",
		"undici": "^7.19.1",
		"vscode-jsonrpc": "^8.2.1",
		"yaml": "^2.8.2"
	},
	"overrides": {
		"rimraf": "6.1.2"
	},
	"devDependencies": {
		"@types/diff": "^7.0.2",
		"@types/hosted-git-info": "^3.0.5",
		"@types/ms": "^2.1.0",
		"@types/node": "^24.3.0",
		"@types/proper-lockfile": "^4.1.4",
		"shx": "^0.4.0",
		"typescript": "^5.7.3",
		"vitest": "^3.2.4"
	},
	"keywords": [
		"coding-agent",
		"ai",
		"llm",
		"cli",
		"tui",
		"agent"
	],
	"author": "Neosantara",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/neosantara-xyz/neo-code.git",
		"directory": "packages/coding-agent"
	},
	"engines": {
		"node": ">=20.6.0"
	},
	"neosantaraConfig": {
		"configDir": ".neo-code"
	},
	"publishConfig": {
		"registry": "https://npm.pkg.github.com"
	}
}
